import KitFile from '../files/KitFile';
export default class KitController {
    unpack(url: string, workingDirPath: string, config: KitFile): Promise<void | never>;
    verifyRepo(url: string): Promise<KitFile | never>;
}
